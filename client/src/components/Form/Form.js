import React  from 'react';
import useStyles from './styles'
const  classes = useStyles();

const Form = () => {
    const  classes = useStyles();
    return (
        <h1>FORM</h1>
    )
}

export default Form;